import React from 'react';
import { storiesOf } from '@storybook/react-native';
import { LoadingView } from '../../../src/views';

storiesOf('LoadingView', module).add('default', () => <LoadingView />);
