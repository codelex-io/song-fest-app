import React from 'react';
import { storiesOf } from '@storybook/react-native';
import { MarkdownEvent } from '../../../src/views';

storiesOf('MarkdownEvent', module).add('list', () => <MarkdownEvent />);
